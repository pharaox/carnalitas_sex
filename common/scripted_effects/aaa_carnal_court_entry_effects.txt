# Dummy sex scene effect, overridden by CBO
carnal_court_sex_scene_effect = {
	if = {
		limit = {
			exists = $PLAYER$
			exists = $TARGET$
			exists = $STRESS_EFFECTS$
			exists = $DRAMA$
		}

		carnx_debug_log_effect = { MSG = carnx_sex_debug_msg_carnal_court_sex_scene_requested }
	}
}
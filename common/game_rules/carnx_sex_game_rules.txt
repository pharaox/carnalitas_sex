carnx_sex_requires_same_location = {
	categories = {
		carnx_carnalitas_sex
	}

	default = carnx_sex_requires_same_location_enabled

	carnx_sex_requires_same_location_disabled = {
		flag = GG_can_change_rule
	}

	carnx_sex_requires_same_location_enabled = {
		flag = GG_can_change_rule
	}
}

carnx_sex_interaction_effect_cd = {
	categories = {
		carnx_carnalitas_sex
	}

	default = carnx_sex_interaction_effect_cd_enabled

	carnx_sex_interaction_effect_cd_disabled = {
		flag = GG_can_change_rule
	}

	carnx_sex_interaction_effect_cd_enabled = {
		flag = GG_can_change_rule
	}
}

carnx_sex_interaction_cd_recipient = {
	categories = {
		carnx_carnalitas_sex
	}

	default = carnx_sex_interaction_cd_recipient_disabled

	carnx_sex_interaction_cd_recipient_disabled = {
		flag = GG_can_change_rule
	}

	carnx_sex_interaction_cd_recipient_enabled = {
		flag = GG_can_change_rule
	}
}

carnx_sex_demand_sex_from_hooked_characters = {
	categories = {
		carnx_carnalitas_sex
	}

	default = carnx_sex_demand_sex_from_hooked_characters_disabled

	carnx_sex_demand_sex_from_hooked_characters_disabled = {
		flag = GG_can_change_rule
	}

	carnx_sex_demand_sex_from_hooked_characters_strong_hooks = {
		flag = GG_can_change_rule
	}

	carnx_sex_demand_sex_from_hooked_characters_all_hooks = {
		flag = GG_can_change_rule
	}
}

carnx_sex_demand_sex_consumes_slave_hooks = {
	categories = {
		carnx_carnalitas_sex
	}

	default = carnx_sex_demand_sex_consumes_slave_hooks_enabled

	carnx_sex_demand_sex_consumes_slave_hooks_disabled = {
		flag = GG_can_change_rule
	}

	carnx_sex_demand_sex_consumes_slave_hooks_enabled = {
		flag = GG_can_change_rule
	}
}

carnx_sex_memories = {
	categories = {
		carnx_carnalitas_sex
	}

	default = carnx_sex_memories_disabled

	carnx_sex_memories_disabled = {
		flag = GG_can_change_rule
	}

	carnx_sex_memories_enabled = {
		flag = GG_can_change_rule
	}
}